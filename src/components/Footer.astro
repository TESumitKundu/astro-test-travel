---
import site from '../../config/site.json';
import { withBase } from '../lib/paths';
const links = Object.entries(site.socialLinks).filter(([, href]) => href);
const socialIcons: Record<string, string> = {
  facebook: 'f',
  instagram: 'ig',
  twitter: 'x',
  linkedin: 'in'
};
---

<footer class="site-footer">
  <div class="container footer-shell">
    <div class="footer-top">
      <a class="footer-brand" href={withBase('/')}>
        <img src={withBase(site.logo)} alt="" width="38" height="38" />
        <span>{site.siteName}</span>
      </a>
      <p>{site.siteDescription}</p>
    </div>

    <div class="footer-columns">
      <div class="footer-links">
        <h2>Explore</h2>
        <a href={withBase('/')}>Home</a>
        <a href={withBase('/tours/')}>Tours</a>
        <a href={withBase('/about/')}>About</a>
        <a href={withBase('/contact/')}>Contact</a>
      </div>
      <div class="footer-links">
        <h2>Contact</h2>
        <a href={`mailto:${site.email}`}>{site.email}</a>
        <a href={`tel:${site.phone}`}>{site.phone}</a>
      </div>
      <div class="footer-links">
        <h2>Social media</h2>
        <div class="social-list" aria-label="Social media links">
          {links.map(([name, href]) => (
            <a href={href} aria-label={name}>
              <b aria-hidden="true">{socialIcons[name] ?? name.slice(0, 2)}</b>
              <span>{name}</span>
            </a>
          ))}
        </div>
      </div>
    </div>
  </div>
  <div class="container copyright">
    <span>&copy; {new Date().getFullYear()} {site.siteName}. Tours managed with Git-based content.</span>
  </div>
</footer>

<style>
  .site-footer {
    background:
      radial-gradient(circle at 12% 0%, rgba(246, 183, 60, 0.16), transparent 20rem),
      linear-gradient(135deg, #102f2b 0%, #111827 100%);
    color: #f9fafb;
  }

  .site-footer .footer-shell {
    display: grid;
    gap: 34px;
    padding: 52px 0;
  }

  .site-footer .footer-top {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
    padding-bottom: 28px;
    border-bottom: 1px solid rgba(255, 255, 255, 0.12);
  }

  .site-footer .footer-columns {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 28px;
  }

  .footer-brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: #fff;
    font-weight: 800;
    text-decoration: none;
  }

  p {
    max-width: 460px;
    margin: 0;
    color: #cbd5e1;
  }

  h2 {
    margin: 0 0 12px;
    font-size: 1rem;
  }

  a {
    display: inline-flex;
    width: fit-content;
    color: #cbd5e1;
    text-decoration: none;
    text-transform: capitalize;
  }

  a + a {
    margin-top: 8px;
  }

  .site-footer .footer-links {
    display: grid;
    align-content: start;
    gap: 8px;
  }

  .site-footer .social-list {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .site-footer .social-list a {
    align-items: center;
    gap: 7px;
    padding: 6px 10px 6px 6px;
    border: 1px solid rgba(255, 255, 255, 0.18);
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.08);
    color: #fff;
    font-size: 0.88rem;
    font-weight: 800;
  }

  .site-footer .social-list b {
    display: inline-grid;
    place-items: center;
    min-width: 28px;
    height: 28px;
    border-radius: 50%;
    background: var(--accent);
    color: #fff;
    font-size: 0.72rem;
    line-height: 1;
    text-transform: uppercase;
  }

  .copyright {
    padding: 18px 0;
    border-top: 1px solid rgba(255, 255, 255, 0.12);
    color: #94a3b8;
    font-size: 0.92rem;
  }

  @media (max-width: 760px) {
    .site-footer .footer-top {
      align-items: flex-start;
      flex-direction: column;
    }

    .site-footer .footer-columns {
      grid-template-columns: 1fr;
    }
  }
</style>
